name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Run
        run: |
          /bin/bash $GITHUB_WORKSPACE/setup/setup-remote.sh
          echo "todo: Add tests" 
